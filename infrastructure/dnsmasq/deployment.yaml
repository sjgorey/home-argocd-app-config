apiVersion: apps/v1
kind: Deployment
metadata:
  name: dnsmasq
  namespace: default
  labels:
    app: dnsmasq
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: dnsmasq
  template:
    metadata:
      labels:
        app: dnsmasq
    spec:
      # Only run on x86_64 nodes to avoid ARM compatibility issues
      nodeSelector:
        kubernetes.io/arch: amd64
      
      # Anti-affinity to spread pods across different x86_64 nodes
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - dnsmasq
              topologyKey: kubernetes.io/hostname
      
      serviceAccountName: default
      
      containers:
      - name: dnsmasq
        image: jpillora/dnsmasq:latest
        imagePullPolicy: Always
        
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 50m
            memory: 64Mi
        
        ports:
        - name: dns-tcp
          containerPort: 53
          protocol: TCP
        - name: dns-udp
          containerPort: 53
          protocol: UDP
        
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - "dig @127.0.0.1 google.com +short +timeout=2 || nslookup google.com 127.0.0.1"
          initialDelaySeconds: 15
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        
        readinessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - "dig @127.0.0.1 google.com +short +timeout=2 || nslookup google.com 127.0.0.1"
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 2
        
        volumeMounts:
        - name: config
          mountPath: /etc/dnsmasq.conf
          subPath: dnsmasq.conf
          readOnly: true
        - name: cache-storage
          mountPath: /var/lib/dnsmasq
        - name: log-storage
          mountPath: /var/log
        
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
      
      volumes:
      - name: config
        configMap:
          name: dnsmasq-config
      - name: cache-storage
        emptyDir: {}
      - name: log-storage
        emptyDir: {}