# This is a template file - DO NOT commit real secrets to public repos
# Create the actual secret manually with:
# kubectl create secret generic paperless-secrets -n paperless-ngx \
#   --from-literal=secret-key="your-django-secret-key" \
#   --from-literal=admin-user="admin" \
#   --from-literal=admin-password="your-secure-password" \
#   --from-literal=admin-email="admin@example.com" \
#   --from-literal=db-name="paperless" \
#   --from-literal=db-user="paperless" \
#   --from-literal=db-password="your-db-password"

# Or create this secret.yaml with your actual values:
#apiVersion: v1
#kind: Secret
#metadata:
#  name: paperless-secrets
#  namespace: paperless-ngx
#  labels:
#    app: paperless-ngx
#type: Opaque
#data:
#  # Base64 encoded values - replace with your actual secrets
#  # echo -n "your-secret-key" | base64
#  secret-key: <base64-encoded-secret-key>
#  # echo -n "admin" | base64  
#  admin-user: <base64-encoded-admin-user>
#  # echo -n "password" | base64
#  admin-password: <base64-encoded-admin-password>
#  # echo -n "admin@example.com" | base64
#  admin-email: <base64-encoded-admin-email>
#  # PostgreSQL credentials
#  # echo -n "paperless" | base64
#  db-name: <base64-encoded-db-name>
#  # echo -n "paperless" | base64
#  db-user: <base64-encoded-db-user>
#  # echo -n "paperless123" | base64
#  db-password: <base64-encoded-db-password>