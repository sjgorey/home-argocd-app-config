# File 1: xmrig-secret.yaml   ← commit this (no actual secret inside!)
apiVersion: v1
kind: Secret
metadata:
  name: xmrig-secrets
  namespace: mining
type: Opaque
stringData:
  wallet: ""          # ← intentionally blank, filled by kubectl create above
  worker-name: "k3s-arm64"