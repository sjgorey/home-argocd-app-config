# 12-firefly-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: firefly-iii
  namespace: firefly-iii
spec:
  selector:
    app: firefly-iii
  ports:
    - name: web
      port: 80
      targetPort: 8080
  type: ClusterIP
  