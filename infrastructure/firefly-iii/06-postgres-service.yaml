# 06-postgres-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: firefly-iii
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # Headless for StatefulSet