---
# ClusterIP Service (internal cluster access)
apiVersion: v1
kind: Service
metadata:
  name: unbound
  namespace: default
  labels:
    app: unbound
spec:
  type: ClusterIP
  selector:
    app: unbound
  ports:
  - name: dns-tcp
    port: 53
    targetPort: 53
    protocol: TCP
  - name: dns-udp
    port: 53
    targetPort: 53
    protocol: UDP

---
# LoadBalancer Service (primary access)
apiVersion: v1
kind: Service
metadata:
  name: unbound-lb
  namespace: default
  labels:
    app: unbound
  annotations:
    metallb.universe.tf/address-pool: default
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.1.12
  selector:
    app: unbound
  ports:
  - name: dns-tcp
    port: 53
    targetPort: 53
    protocol: TCP
  - name: dns-udp
    port: 53
    targetPort: 53
    protocol: UDP

---
# NodePort Service (for router access with port forwarding)
apiVersion: v1
kind: Service
metadata:
  name: unbound-nodeport
  namespace: default
  labels:
    app: unbound
spec:
  type: NodePort
  selector:
    app: unbound
  ports:
  - name: dns-tcp
    port: 53
    targetPort: 53
    nodePort: 30053
    protocol: TCP
  - name: dns-udp
    port: 53
    targetPort: 53
    nodePort: 30053
    protocol: UDP