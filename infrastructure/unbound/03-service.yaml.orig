apiVersion: v1
kind: Service
metadata:
  name: unbound-lb
  namespace: unbound
spec:
  type: LoadBalancer
  selector:
    app: unbound
  ports:
    - name: dns-tcp
      port: 53
      protocol: TCP
    - name: dns-udp
      port: 53
      protocol: UDP
