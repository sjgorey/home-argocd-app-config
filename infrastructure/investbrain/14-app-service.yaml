# 14-app-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: investbrain
  namespace: investbrain
spec:
  selector:
    app: investbrain
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
